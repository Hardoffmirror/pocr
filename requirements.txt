# PoE Recombinator Simulator Dependencies

# Графика и визуализация
matplotlib>=3.5.0

# Tkinter обычно входит в стандартную библиотеку Python,
# но если его нет, установите python3-tk через системный пакетный менеджер:
# sudo apt-get install python3-tk  (Debian/Ubuntu)
# sudo yum install python3-tkinter  (RedHat/Fedora)
# brew install python-tk  (macOS)
